import { createAction } from '@ngrx/store';

export const login = createAction('[Counter Component] Increment');
export const register = createAction('[Counter Component] Decrement');
export const reset = createAction('[Counter Component] Reset');